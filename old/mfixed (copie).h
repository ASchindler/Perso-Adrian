//#include "systemc.h"

class mfixed {
  private:
    short h;
    unsigned short l;
  public:

    // constructor
    mfixed ();

    //mfixed(int, double );

/*
    inline bool operator == (const mfixed & rhs) const;

    inline mfixed& operator = (const mfixed& rhs);

    inline friend void sc_trace(sc_trace_file *tf, const mfixed & v,
				const std::string & NAME );

    inline friend ostream& operator << ( ostream& os,  mfixed const & v );


    inline void fx_copy (mfixed A, mfixed B);


// fixed point multiplication
    friend inline mfixed fx_mul  (mfixed A, mfixed B);

// fixed point addition
    friend inline mfixed fx_add (mfixed A, mfixed B);

    friend mfixed operator + (const mfixed& rhs);
*/

};

//mfixed operator+ (const mfixed& rhs) const; 

//mfixed mfixed1 (int A);
